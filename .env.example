# Dinner Bell — Environment Variables
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to git

# =============================================================================
# CONVEX DATABASE
# =============================================================================
# Get these from your Convex dashboard: https://dashboard.convex.dev
# After running `npx convex dev` for the first time, these will be auto-configured

CONVEX_DEPLOYMENT=dinner-bell-ver-01
NEXT_PUBLIC_CONVEX_URL=https://avid-skunk-642.convex.cloud

# =============================================================================
# AI / LLM (OpenRouter)
# =============================================================================
# Get your API key from: https://openrouter.ai/keys
# Used for: Zylo chat, weekly plan generation, fandom voice

OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Default model for Zylo and AI features
# Using Gemini 3 Flash: fast, intelligent, excellent tool calling
OPENROUTER_DEFAULT_MODEL=google/gemini-3-flash-preview

# =============================================================================
# AUTHENTICATION (Convex Auth + Google OAuth)
# =============================================================================
# Google OAuth: Create credentials at https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID (Web application)
# 2. Add authorized redirect URI: https://your-project.convex.cloud/api/auth/callback/google

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# OPTIONAL: Push Notifications (for PWA)
# =============================================================================
# Generate VAPID keys: npx web-push generate-vapid-keys
# Only needed when implementing the Notifications section

# NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-public-key
# VAPID_PRIVATE_KEY=your-private-key
# VAPID_EMAIL=mailto:your-email@example.com
