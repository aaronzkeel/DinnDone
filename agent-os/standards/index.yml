# DinnDone Standards Index
# Auto-generated by /agent-os:discover-standards

convex:
  date-handling:
    description: Date formatting to avoid timezone bugs
  indexes:
    description: Database indexes for efficient queries
  queries-mutations:
    description: Convex query and mutation patterns
  schema-validators:
    description: Reusable validators in schema.ts

css:
  button-semantics:
    description: Button colors by action type (green=go, gold=change, red=danger)
  color-variables:
    description: CSS custom properties for colors, not Tailwind or hex
  fonts:
    description: Lora for headings, Plus Jakarta Sans for body

global:
  locked-files:
    description: Files requiring approval before modification
  time-formatting:
    description: Convert timestamps to relative time ("5m ago")

nextjs:
  app-shell:
    description: Header + main + bottom nav layout structure
  page-structure:
    description: Page container pattern with RequireAuth
  view-state:
    description: Internal view state for multi-step flows

react:
  accessibility:
    description: Touch targets, aria labels, keyboard support
  auto-resize-textarea:
    description: Text input that grows with content up to max height
  client-components:
    description: When to use "use client" directive
  effort-dots:
    description: Visual 1-3 dot indicator for effort level
  empty-state:
    description: Centered icon + heading + CTA for empty lists
  focus-trap-modal:
    description: Keep keyboard focus inside modals
  loading-dots:
    description: Bouncing dots animation for AI thinking state
  props-types:
    description: Shared types in src/types/ folder
  toast-notifications:
    description: Auto-dismiss temporary messages
